{
  "ci": {
    "assert": {
      "assertions": {
        "categories:performance": ["warn", {"minScore": 0.9}],
         "interactive": ["error", {"maxNumericValue": 10000}],
         "cumulative-layout-shift": ["error", {"maxScore": 0.1}],
         "largestContentfulPaint":  ["error", {"maxNumericValue": 2500}]
      }
    },
    "collect": {
      "settings": {
        "chromeFlags": ["--disable-gpu", "--no-sandbox", "--no-zygote"]
      }
    }
  }
}
