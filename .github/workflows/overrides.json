{
  "config": {
    "settings": {
      "onlyCategories": ["performance"],
      onlyAudits: [
      'first-meaningful-paint',
      'speed-index',
      'interactive',
    ]
    }
  },
  "options": {
    "chromeFlags": [
      "--disable-dev-shm-usage",
      "--headless",
      "--no-sandbox",
      "--ignore-certificate-errors"
    ]
  }
}
